1. get [cli](https://github.com/Tyrrrz/DiscordChatExporter/releases/latest)
2. get token, channel id (see cli guide):

 ```bash
$ ./DiscordChatExporter.Cli guide
``` 

3. get archive:

```bash
$ ./DiscordChatExporter.Cli export -t <token> -c <channel_id>
 ```
